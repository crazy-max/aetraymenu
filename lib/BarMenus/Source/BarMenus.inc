{ Compatibility mode }

{ By defining this, compatibilty layer class is installed along the
  new class. The old class is depricated, but yet supported in this
  version. This is not defined by default. }

{.$DEFINE OLDCLASS}
